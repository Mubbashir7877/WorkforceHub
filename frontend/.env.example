# Copy this to .env in the frontend/ directory and set the correct backend URL.
# Vite reads .env automatically during development and build.
#
# The variable MUST start with VITE_ to be exposed to browser code.

VITE_API_BASE_URL=http://localhost:8080/api/v1
